# SVG icon set — stroke icons, no fill, scaled to viewBox="0 0 24 24".
# Each value is a complete <svg> string ready to load into a QSvgRenderer
# or write to a temp file for QIcon.
# Populated as icons are needed per milestone; empty dict is the valid
# starting state — callers should check for key existence before using.

ICONS: dict[str, str] = {}
